/**
 * IPC-specific metadata types.
 * Used by @IpcChannel and @IpcResult parameter decorators.
 */

// No specific types needed for IPC - just parameter indices
// which are handled by the base storage methods

// Export empty object to make this a valid module
export {};
