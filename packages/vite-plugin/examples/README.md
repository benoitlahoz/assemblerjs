# Example Usage

This directory contains example usage of `vite-plugin-assemblerjs`.

## Basic Example

See `vite.config.example.ts` for a minimal configuration example.

## Testing

The example can be tested by:

1. Installing dependencies
2. Running the build
3. Verifying the output

```bash
cd packages/vite-plugin
yarn test
```
